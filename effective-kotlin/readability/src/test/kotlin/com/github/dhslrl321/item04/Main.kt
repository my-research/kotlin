package com.github.dhslrl321.item04

import io.kotest.core.spec.style.StringSpec

/**
 * 아이템 4. inferred 타입으로 리턴하지 말라
 *
 *
 * 타입 추론은 interface 나 super class 에 맞춰서 되지 않고 정확히 오른쪽에 있는 피연산자에 맞는 타입으로 설정됨
 */
class Main: StringSpec({
  "일반적으로 타입 추론은 정확히 오른쪽 피연산자에 맞는 타입이 나옴" {
  }
})



