package nullablewithjava

/**
 * nullable 과 자바
 *
 * 코틀린은 자바의 상호운용성에 대해서 자랑스럽게 이야기하는 언어
 * 자바의 타입 시스템에는 nullable 에 대한 힌트를 언어단에서 지원하지 않음
 *      -> 그래서 자바 타입을 사용할 때는 코틀린이 nullable 에 대한 판단이 불가능함
 *      -> 대신 어노테이션으로 확인 가능 `@Nullable`, `@NotNull` 등등
 *
 * `@Nullable` + Type => Type?
 * `@NotNull` + Type => Type
 */
fun main() {

}